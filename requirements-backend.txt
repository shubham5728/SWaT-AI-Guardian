# Minimal deps for the WebSocket streaming backend (src/api/ws_server.py).
# Trimmed from requirements.txt — no streamlit/pyspark/jupyter/plotting — so the
# Docker image builds faster.
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2
tensorflow-cpu==2.15.0
kafka-python==2.0.2
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.2
joblib==1.3.2
pyyaml==6.0.1
python-json-logger==2.0.7
python-dotenv==1.0.0
